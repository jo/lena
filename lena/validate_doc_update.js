function(doc, oldDoc, userCtx, secObj) {
  // reject non admins
  // check for type field: 'page'
  // check for folder: must be present and must not contain a '/' (and maybe a ':'?)
  // check title: must be present
  // check subtitle
  // check aside
  // check article
  // check attachments
}
